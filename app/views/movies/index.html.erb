<%= button_to "Discover Page", "/users/#{@user.id}/discover", method: :get %>
<% @movies.each do |movie| %>
  <div>
    Title: <%= link_to movie[:title], user_movies_path(movie) %>
    Vote Average: <%= movie[:vote_average] %>
    <% end %>
    